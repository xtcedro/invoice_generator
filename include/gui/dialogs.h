#ifndef DIALOGS_H
#define DIALOGS_H

#include <gtk/gtk.h>

// Function to show the About dialog
void show_about_dialog(GtkWindow *parent_window);

// Function to show the Preferences dialog
void show_preferences_dialog(GtkWindow *parent_window);

#endif // DIALOGS_H
